# HealthInformationLanding

<!-- STORY -->

## Introduction

HealthInformationLanding is an easy-to-use component.

## Usage

```javascript
import { HealthInformationLanding } from 'some-package-name';
```

## Example use

```javascript
const myPage = props => {
  return (
    <main>
      <HealthInformationLanding />
    </main>
  );
};
```

## Properties

- `className` - is the class name of the component

| propName  | propType | defaultValue | isRequired |
| --------- | -------- | ------------ | ---------- |
| className | string   | -            | -          |
